[project]
name = "misc"
version = "0.1.0"
description = "misc"
readme = "README.md"
requires-python = ">=3.9, <4.0"

[tool.nonebot]
adapters = [
    { name = "Console", module_name = "nonebot.adapters.console" }
]
plugins = []
plugin_dirs = []
builtin_plugins = ["echo"]
